/*
 * A coord object represents a single row,column pair for a shell is used to 
 * make JSON strings that represent a shell.
 *
 */

function coord(row1,col1) {
    this.row : row1;
    this.col : col1;
}